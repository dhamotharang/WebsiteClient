import { GhmMaskDirective } from './ghm-mask.directive';

describe('GhmMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new GhmMaskDirective();
    expect(directive).toBeTruthy();
  });
});
