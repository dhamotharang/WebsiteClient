import { GhmCurrencyPipe } from './ghm-currency.pipe';

describe('GhmCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new GhmCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
