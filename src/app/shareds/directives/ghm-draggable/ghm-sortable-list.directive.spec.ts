import { GhmSortableListDirective } from './ghm-sortable-list.directive';

describe('GhmSortableListDirective', () => {
  it('should create an instance', () => {
    const directive = new GhmSortableListDirective();
    expect(directive).toBeTruthy();
  });
});
