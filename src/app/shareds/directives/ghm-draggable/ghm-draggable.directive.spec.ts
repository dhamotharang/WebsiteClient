import { GhmDraggableDirective } from './ghm-draggable.directive';

describe('GhmDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new GhmDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
