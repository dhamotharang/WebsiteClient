import { GhmSortableDirective } from './ghm-sortable.directive';

describe('GhmSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new GhmSortableDirective();
    expect(directive).toBeTruthy();
  });
});
