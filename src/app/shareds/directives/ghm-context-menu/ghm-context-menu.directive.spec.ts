import { GhmContextMenuDirective } from './ghm-context-menu.directive';

describe('GhmContextMenuDirective', () => {
    it('should create an instance', () => {
        const directive = new GhmContextMenuDirective();
        expect(directive).toBeTruthy();
    });
});
