export interface LanguageSearchViewModel {
    languageId: string;
    name: string;
    description: string;
    isDefault: boolean;
    isActive: boolean;
}

export interface LanguageViewModel {
    id: string;
    name: string;
    isSelected: boolean;
}
