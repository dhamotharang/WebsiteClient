import { GhmAmountToWordPipe } from './ghm-amount-to-word.pipe';

describe('NhAmountToWordPipe', () => {
  it('create an instance', () => {
    const pipe = new GhmAmountToWordPipe();
    expect(pipe).toBeTruthy();
  });
});
